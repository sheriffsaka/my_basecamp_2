<td>
    <%= link_to user.email, user %>
</td>
<td>
    <%= form_for(user) do |f| %>
        <%= f.select(:role, User.roles.keys.map { |role| [role.titleize, role] }, {}, class: "form-select form-select-sm") %>
        <td>
        <%= f.submit('Change Role', class: "btn btn-secondary") %>
        </td>
    <% end %>

</td>
<td>
    <%# <%= link_to("Delete User", user_path(user), :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn btn-danger') unless user == current_user %>
    <%= link_to "Delete User", user_path(user), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class:"btn btn-danger" %>
</td>